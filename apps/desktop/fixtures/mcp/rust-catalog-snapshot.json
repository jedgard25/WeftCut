{
  "tools": [
    {
      "name": "ping",
      "description": "Liveness check. Returns 'pong' to confirm the WeftCut MCP server is reachable.",
      "inputSchema": {
        "type": "object"
      }
    },
    {
      "name": "apply_subtitles",
      "description": "Import a subtitle document (SRT/VTT/ASS) as editable Text layers on the caption tracks. Cue timings come from the body; each cue packs onto the first unlocked caption track with room, and a new caption track opens only for a cue that collides with all of them. `format` is sniffed when omitted; advanced ASS styling (karaoke, drawings) is simplified. For a `transcribe_clip` result use `apply_transcripts` instead — an SRT discards its word timing. Returns the id of the caption track the first cue landed on.",
      "inputSchema": {
        "properties": {
          "body": {
            "description": "Subtitle document body (SRT, ASS, or VTT).",
            "type": "string"
          },
          "format": {
            "description": "'srt', 'ass', or 'vtt'. Sniffed from body when omitted.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "body"
        ],
        "type": "object"
      }
    },
    {
      "name": "detect_pauses",
      "description": "Find the pauses in a clip's audio — the stretches nobody is speaking — from the pre-computed waveform. Read-only: the write is `remove_pauses` (cut them) or `add_marker` (mark them). A pause is a run where every channel's peak stays below `threshold_amp` for at least `min_pause_us`; a loud run shorter than `bridge_us` inside it (a click, a cough) does not end it. Defaults `threshold_amp=0.02` (-34 dBFS), `min_pause_us=500000`, `bridge_us=80000` (must be below `min_pause_us`). `layer_id` is an Audio layer, or a VideoClip, which resolves to the Audio layer of its link (refused when it plays no sound). Returns `{ pauses: [{ t_start_us, t_end_us }], noise_floor_amp, peaks_source }`, timeline-absolute µs, sorted; `noise_floor_amp` is the 10th-percentile peak (a threshold reads well at the floor + 6 dB); `peaks_source` is \"raw\" or \"fx\" (baked effect chain). Errors until the waveform job finishes — wait for `media:job_complete` with `kind=waveform` and retry.",
      "inputSchema": {
        "properties": {
          "bridge_us": {
            "description": "A loud run shorter than this (µs) inside a quiet run does not end the\npause. Default 80000 (80ms). Must be ≥ 0 and below `min_pause_us`.",
            "type": [
              "integer",
              "null"
            ]
          },
          "layer_id": {
            "description": "Target Audio layer id. A VideoClip id is accepted by the host, which\nresolves it to the Audio layer of its link before the call.",
            "type": "string"
          },
          "min_pause_us": {
            "description": "Shortest pause to surface, in microseconds. Default 500000 (0.5s).",
            "type": [
              "integer",
              "null"
            ]
          },
          "threshold_amp": {
            "description": "Peak amplitude threshold in [0.0, 1.0]. Anything strictly below this\ncounts as quiet. Default 0.02 (≈ -34 dBFS).",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "analyze_clip",
      "description": "Detect shot boundaries in a VideoClip layer. Deterministic, over the source (720p proxy preferred). Returns `{ shots: [{ index, t_start_us, t_end_us, keyframe_t_us, brightness, motion, sharpness, flags }], cut_scores: [{ t_us, score }] }`, all SOURCE-ABSOLUTE µs clipped to the layer's source window. `shots` is the cleaned segmentation (cuts closer than `min_shot_us` merged), `cut_scores` the raw cut signal (0..1). Per shot: `keyframe_t_us` is the midpoint cover frame; `brightness` mean luma 0..1; `sharpness` variance of the Laplacian (higher = sharper); `motion` 0..1 endpoint difference; `flags` may hold \"black\", \"freeze\", \"fade\". Optional `sensitivity` (0..1, default 0.4; lower = more cuts), `min_shot_us` (default 500000), `passes` (subset of \"shots\" / \"stats\" / \"events\", default all — drop \"stats\" / \"events\" for timing only). VideoClip layers only. To split at every cut in one edit use `auto_split_by_shot`.",
      "inputSchema": {
        "properties": {
          "layer_id": {
            "description": "Target VideoClip layer id.",
            "type": "string"
          },
          "min_shot_us": {
            "description": "Minimum shot duration (microseconds); cuts closer than this are merged.\nDefault 500000 (0.5 seconds).",
            "type": [
              "integer",
              "null"
            ]
          },
          "passes": {
            "description": "Which passes to run — a subset of `[\"shots\", \"stats\", \"events\"]`.\nDefault all. Drop `\"stats\"` / `\"events\"` to skip per-shot frame sampling\nand return timing only.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "sensitivity": {
            "description": "Cut sensitivity in [0.0, 1.0]: a frame whose shot-change score exceeds\nthis starts a new shot. Lower = more (finer) cuts. Default 0.4.",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "compare_frames",
      "description": "Compare two video frames for perceptual similarity — dedup shots, match a cutaway. Each side is `{ layer_id, t_us }`: a VideoClip layer and a SOURCE-ABSOLUTE µs timestamp (the space `analyze_clip`'s `keyframe_t_us` and `media://{id}/frame/<t_us>` use); the two may be the same clip or different clips. Returns `{ phash_hamming, ssim, similar }`: `phash_hamming` is the 0..64 distance between the frames' perceptual hashes (0 = identical, 20+ = a different scene), `ssim` is structural similarity 0..1, and `similar` is `phash_hamming <= 10 && ssim >= 0.5`. Read-only; VideoClip layers only, errors naming the offending side.",
      "inputSchema": {
        "properties": {
          "a": {
            "description": "First frame to compare.",
            "properties": {
              "layer_id": {
                "description": "Target VideoClip layer id.",
                "type": "string"
              },
              "t_us": {
                "description": "SOURCE-ABSOLUTE microseconds of the frame to sample (the space\n`analyze_clip`'s `keyframe_t_us` uses).",
                "type": "integer"
              }
            },
            "required": [
              "layer_id",
              "t_us"
            ],
            "type": "object"
          },
          "b": {
            "description": "Second frame to compare.",
            "properties": {
              "layer_id": {
                "description": "Target VideoClip layer id.",
                "type": "string"
              },
              "t_us": {
                "description": "SOURCE-ABSOLUTE microseconds of the frame to sample (the space\n`analyze_clip`'s `keyframe_t_us` uses).",
                "type": "integer"
              }
            },
            "required": [
              "layer_id",
              "t_us"
            ],
            "type": "object"
          }
        },
        "required": [
          "a",
          "b"
        ],
        "type": "object"
      }
    },
    {
      "name": "import_media",
      "description": "Import a media file from an absolute path. Hashes the file (blake3) and probes metadata via ffprobe when installed. Returns the new media id.",
      "inputSchema": {
        "properties": {
          "path": {
            "description": "Absolute path to a video / audio / image / subtitle file the host can read.",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      }
    },
    {
      "name": "extract_clip_audio",
      "description": "Extract a VideoClip or Audio layer's ORIGINAL source audio — before gain, mute, effects or mixing — for an agent running its own speech model; no engine or API key is involved and nothing is uploaded. Returns a JSON metadata block (`layer_id`, `media_id`, `t_start_us`, `t_end_us`, `source_in_us`, `source_out_us`, `duration_us`, `sample_rate_hz`, `channels`, `bits_per_sample`, `byte_length`, `mime_type`) plus an MCP audio block: base64 WAV, mono 16 kHz 16-bit, starting at zero. Optional `t_start_us`/`t_end_us` are composition-absolute µs, defaulting to the layer endpoints; at most 60 s per call — walk a long clip in consecutive windows. Add the reported `t_start_us` to the offsets your model returns before `apply_subtitles`. Refuses a layer with no audio, a window outside the layer, and a VideoClip with speed != 1.0 (a VideoClip's audio is its own stream, not its linked Audio layer).",
      "inputSchema": {
        "properties": {
          "layer_id": {
            "description": "Target VideoClip or Audio layer id.",
            "type": "string"
          },
          "t_end_us": {
            "description": "Exclusive window end in the owning composition's absolute microseconds.\nDefaults to the layer end. Maximum window duration: 60000000 us (60 s).",
            "type": [
              "integer",
              "null"
            ]
          },
          "t_start_us": {
            "description": "Window start in the owning composition's absolute microseconds.\nDefaults to the layer start. Must lie inside the layer.",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "transcribe_clip",
      "description": "Transcribe a VideoClip or Audio layer with the configured engine (cloud OpenAI Whisper, or local whisper.cpp / FunASR). Returns `{ backend, segments: [{ t_start_us, t_end_us, text, words: [{ t_start_us, t_end_us, text }] }], language, word_timing, srt }` in timeline-absolute µs. Hand the envelope to `apply_transcripts` to lay captions that keep the word timing (`apply_subtitles` with `srt` works but discards it). `word_timing` is \"exact\" or \"interpolated_from_cue\". Optional `t_start_us`/`t_end_us` narrow the window; `backend` (\"openai\" | \"whisper_cpp\" | \"funasr\") REQUIRES that engine — it errors naming the missing key / binary / model rather than substituting, so a local choice never uploads; omitted, the user's preferred engine then availability. `word_timestamps` (default true) asks for exact per-word times where the engine can (OpenAI is SRT-only). `segment` (default \"engine\") for sentences; persists per source: `media://{id}/transcript`. A VideoClip with speed != 1.0 is refused. Errors name the cause: no engine, the provider cap (~13 min for cloud Whisper), rate limits, auth.",
      "inputSchema": {
        "properties": {
          "backend": {
            "description": "Strict engine override: `\"openai\"` | `\"whisper_cpp\"` | `\"funasr\"`.\nThat engine serves or the call errors; nothing is substituted. Omitted:\nthe user's preferred engine, then availability.",
            "type": [
              "string",
              "null"
            ]
          },
          "language": {
            "description": "Optional ISO-639-1 language hint (`\"en\"`, `\"zh\"`). Auto-detect when omitted.",
            "type": [
              "string",
              "null"
            ]
          },
          "layer_id": {
            "description": "Target VideoClip or Audio layer id.",
            "type": "string"
          },
          "segment": {
            "description": "`\"engine\"` (default: the engine's own segments) or `\"sentence\"`\n(re-segmented into sentences — merged across sub-pause gaps and terminal\npunctuation, word spans kept — the caption-ready shape for\n`apply_transcripts`). The cache always stores engine segments.",
            "type": [
              "string",
              "null"
            ]
          },
          "t_end_us": {
            "description": "Optional transcription window end in timeline microseconds.\nDefaults to the layer's `t_end_us`. Must lie within the layer.",
            "type": [
              "integer",
              "null"
            ]
          },
          "t_start_us": {
            "description": "Optional transcription window start in timeline microseconds.\nDefaults to the layer's `t_start_us`. Must lie within the layer.",
            "type": [
              "integer",
              "null"
            ]
          },
          "word_timestamps": {
            "description": "Ask for exact per-word times where the engine can emit them. Default\ntrue; `false` forces SRT-style interpolated words. The result's\n`word_timing` reports what you got.",
            "type": [
              "boolean",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "synthesize_speech",
      "description": "Synthesize speech with the configured cloud TTS provider (OpenAI tts-1) and place it as an Audio layer. `text` ≤ 4096 chars; `voice` is one of alloy / echo / fable / onyx / nova / shimmer; optional `speed` 0.25..4.0 (default ≈ 1.0); optional `target_track_id` (default: the first Audio track, else a new 'Voiceover' track); optional `t_start_us` (default: the composition's current duration, so the clip appends at the end). The MP3 is cached by `(model, voice, speed, text)`, so a repeat call costs no API request. Returns `{ layer_id, media_id, t_start_us, t_end_us, cached }`.",
      "inputSchema": {
        "properties": {
          "speed": {
            "description": "0.25..4.0 for tts-1. Omit to use the provider default (~1.0).",
            "type": [
              "number",
              "null"
            ]
          },
          "t_start_us": {
            "description": "Optional timeline start in microseconds. Defaults to the composition's\ncurrent `duration_us` so the voiceover appends at the end.",
            "type": [
              "integer",
              "null"
            ]
          },
          "target_track_id": {
            "description": "Optional Audio track id. If omitted, lands on the first existing Audio\ntrack or auto-creates one labeled \"Voiceover\".",
            "type": [
              "string",
              "null"
            ]
          },
          "text": {
            "description": "Text to synthesize. tts-1 caps at 4096 characters.",
            "type": "string"
          },
          "voice": {
            "description": "Voice identifier. tts-1 accepts: alloy, echo, fable, onyx, nova, shimmer.",
            "type": "string"
          }
        },
        "required": [
          "text",
          "voice"
        ],
        "type": "object"
      }
    },
    {
      "name": "describe_clip",
      "description": "Describe a VideoClip layer's visual content as timestamped, open-vocabulary segments with a video-understanding model (local Qwen3-VL / MiniCPM-V via llama-mtmd-cli, or an OpenAI-compatible endpoint). Samples frames at `fps` (default 1.0) and runs the model once over the window. Returns `{ backend, model, segments: [{ t_start_us, t_end_us, text, tags }] }` in SOURCE-ABSOLUTE µs; `text` is prose, `tags` short visual keywords (subjects, setting, camera, shot type). Cached per source range. Optional `t_start_us`/`t_end_us` narrow the window; `focus` (\"general\" | \"shot-type\") picks the prompt that fills `tags`; `language` is a BCP-47 tag for the output (default: the app's UI language) and part of the cache key. `backend` (\"qwen3_vl\" | \"minicpm_v\" | \"byo_endpoint\") REQUIRES that engine — it errors naming the missing binary / model / endpoint rather than substituting, so a local choice never uploads frames; omitted, the user's preferred engine then availability (local first). A VideoClip with speed != 1.0 is refused; so is a call with no engine configured, naming the gap.",
      "inputSchema": {
        "properties": {
          "backend": {
            "description": "Strict engine override: `\"qwen3_vl\"` | `\"minicpm_v\"` | `\"byo_endpoint\"`.\nThat engine serves or the call errors; nothing is substituted. Omitted:\nthe user's preferred engine, then availability.",
            "type": [
              "string",
              "null"
            ]
          },
          "focus": {
            "description": "`\"general\"` or `\"shot-type\"` (biases `tags` toward shot type / camera).\nDefault: the app's setting, else `\"general\"`. Cache key.",
            "type": [
              "string",
              "null"
            ]
          },
          "fps": {
            "description": "Frames sampled per second (higher = finer, costlier; capped to the\nmodel's context). Default: the app's setting, else 1.0. Cache key.",
            "type": [
              "number",
              "null"
            ]
          },
          "language": {
            "description": "BCP-47 tag the `text` and `tags` come back in (`\"en-US\"`, `\"zh-CN\"`,\n`\"ja\"`). Default: the app's UI language. Cache key.",
            "type": [
              "string",
              "null"
            ]
          },
          "layer_id": {
            "description": "Target VideoClip layer id.",
            "type": "string"
          },
          "t_end_us": {
            "description": "Optional window end in timeline microseconds. Defaults to the layer's\n`t_end_us`. Must lie within the layer.",
            "type": [
              "integer",
              "null"
            ]
          },
          "t_start_us": {
            "description": "Optional window start in timeline microseconds. Defaults to the layer's\n`t_start_us`. Must lie within the layer.",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "type": "object"
      }
    }
  ],
  "resources": [
    {
      "uri": "project://current",
      "name": "Current project",
      "description": "The full open WeftCut project as JSON. Re-fetch after change events.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://composition",
      "name": "Composition",
      "description": "Canvas size, fps, sample rate, color space, background.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://media",
      "name": "Media pool",
      "description": "All imported media items keyed by id.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://tracks",
      "name": "Tracks",
      "description": "Tracks with layer envelopes. Read project://layers/{id} for full layer detail.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://timeline",
      "name": "Timeline",
      "description": "Compact flat layer rows with a gap list. Takes ?composition=<id>, ?t_start_us=&t_end_us= (window, overlap), ?offset=&limit= (default 200, max 1000). Read project://layers/{id} for full layer detail.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://markers",
      "name": "Markers",
      "description": "Timeline markers, sorted by t_us.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://compositions",
      "name": "Compositions",
      "description": "Every composition — the root and each Group — with id, label, duration_us and ref_count (how many CompositionRef layers place it). project://tracks?composition=<id> and project://markers?composition=<id> read one of them; unscoped they read the root.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://history",
      "name": "History",
      "description": "Recent operations and named checkpoints (no snapshots). `ops` is a window: `window_start` is the absolute stack index of `ops[0]`, and `cursor` is absolute too — never an offset into `ops`; absolute indices are the only ones `jump_to` accepts. `evicted` > 0 means the oldest entries were dropped for good.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://compiled",
      "name": "Audio mix plan",
      "description": "Compiled export-audio mix plan (layer placement on the 48 kHz frame grid + envelope summaries) — for agents that want structural reasoning about what export will mix.",
      "mimeType": "application/json"
    },
    {
      "uri": "composition://meter",
      "name": "Audio meter",
      "description": "Latest preview master-bus level reading (rms/peak dBFS). `live: false` when nothing has played in the last 2 seconds.",
      "mimeType": "application/json"
    }
  ],
  "prompts": [
    {
      "name": "cut-pauses",
      "description": "Cut the pauses out of a clip and close the gaps, tightening it. Or mark them first, to review every pause on the ruler before any of it goes.",
      "arguments": [
        {
          "name": "layer_id",
          "description": "Target Audio layer id, or the VideoClip that plays it — a VideoClip is resolved to the Audio layer of its link.",
          "required": true
        },
        {
          "name": "threshold_amp",
          "description": "Peak amplitude threshold in [0.0, 1.0]. Default 0.02 (≈ -34 dBFS).",
          "required": false
        },
        {
          "name": "min_pause_us",
          "description": "Shortest pause to cut, in microseconds. Default 500000 (0.5s).",
          "required": false
        },
        {
          "name": "pad_us",
          "description": "Microseconds of each pause kept on EACH side when removing. Default 100000 (100ms); 0 erases each pause whole.",
          "required": false
        }
      ]
    },
    {
      "name": "auto-caption",
      "description": "Transcribe a video or audio layer with the configured engine, then lay the transcript on the caption tracks with its word timing.",
      "arguments": [
        {
          "name": "layer_id",
          "description": "Target VideoClip or Audio layer id.",
          "required": true
        },
        {
          "name": "language",
          "description": "Optional ISO-639-1 language hint (en, zh, etc.). Auto-detect when omitted.",
          "required": false
        }
      ]
    },
    {
      "name": "voiceover",
      "description": "Generate cloud TTS for a script and attach it as an Audio layer.",
      "arguments": [
        {
          "name": "script",
          "description": "Text to speak. tts-1 caps a single call at 4096 chars; for longer scripts split into paragraphs.",
          "required": true
        },
        {
          "name": "voice",
          "description": "OpenAI voice: alloy, echo, fable, onyx, nova, or shimmer.",
          "required": false
        },
        {
          "name": "speed",
          "description": "Optional speech speed in [0.25, 4.0]. Omit for the provider default.",
          "required": false
        },
        {
          "name": "target_track_id",
          "description": "Optional Audio track id. Defaults to the first existing Audio track or a new 'Voiceover' track.",
          "required": false
        }
      ]
    }
  ]
}
